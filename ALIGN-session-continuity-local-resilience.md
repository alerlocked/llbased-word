# 需求对齐卡：会话接续 + 本地模型运行时韧性（底层架构更新第一批）

> 2026-08-16 /lead 进入。背景：多人协作开始（同事做登录界面/用户层，本线做底层架构）。DB 走穿透式（全用户可访问），多用户分层不在本 slug。

## 目标

- 解决谁的什么问题：工艺人员在**项目内多轮对话修改工艺文件**，本地 Qwen3-30B-A3B 上下文易满/退化；**新开会话无法接续**之前的修改作业（现状核实：主生成链路不读会话记忆，新会话空白起步）。本地运行时错误裸抛给用户，无自检无缓解。
- 成功长什么样（可观察）：
  - S1 项目内新开会话说"继续改 G25a"，AI 能接上上一会话工作状态（知道改过什么/进行到哪），不要求逐字历史
  - S2 同会话多轮修改不因早轮次丢失而"失忆重做"（上下文组装含工作状态，非仅 last-10×500字）
  - S3 本地 LLM 错误不再裸抛：分类 → 可读提示 + 自动缓解（重试/降级/裁剪重试），日志留痕
  - S4 pytest 718 不回归
  - S5 G25a per-row 生成收尾有完成度核对：n 道工序 vs 有内容行数，缺口显式报（SSE 事件 + 日志分类明细），不再静默空行交付；单行失败先原地重试（2 次退避）

## 边界

- 做：
  - A. 会话接续：后端项目级工作状态记忆（state 写入 + 新会话开局注入主生成链路 + memory 按项目分目录召回）
  - B. LLM 韧性层：llm_service 错误分类 + 缓解策略（重试/降级/裁剪）
  - C. G25a per-row 静默失败治理：单行重试 + 完成度核对显式上报（根因已定位，VISION"可靠"验收补漏）
- 不做：
  - 登录界面/用户体系（同事负责）
  - DB schema 改动/多用户隔离架构（穿透式既定，session 记忆按 project 键控，将来加 user 维度是加列）
  - 前端大改（sessions 结构已有，最多小改）
  - 不换模型/不上云

## 模糊点（2026-08-16 全部清零，用户拍板）

1. 【已澄清】接续深度 → **state + memory 组合**（用户拍）：`state` = 项目级工作状态（当前任务/改到哪/口径偏好，任务级滚动覆盖，新会话全量注入）；`memory` = 会话历史摘要（现有 MemoryService 复用，关键词召回注入）。设计约束（harness E1）：长上下文≠长期记忆，不回放全历史
2. 【已澄清】本地错误实际样本 → **G25a 工序方法表部分行为空**（工序四有/五空/六有，间歇性）。根因代码级定位：`writing_agent.py:1729/1736/1762` 三处单行失败（LLM error / JSON parse 失败 / gather 异常）全部静默 `return []`，不重试不上报。治法 = F1 单行原地重试（2 次退避）+ F4 完成度核对（n 行 vs 有内容行，缺口显式报 SSE）。违反 VISION"可靠"验收（静默假成功），本 slug 一并治
3. 【已拍】协作 git 边界 → **feature 分支 + 标准 PR 流程**（用户拍："走标准 PR 合入流程，要用到 review"），review 用 `/pr-review`
4. 【已拍】memory 落点 → **按项目分目录** `data/memory/projects/{project_id}/`（用户拍），预留 user 维度：将来升级 `projects/{user_id}/{project_id}/`（穿透式 DB 既定，记忆先按 project 键控）
5. 【接受的不确定性】"上下文满"确切报错模式未采到（用户无日志样本）——B 层错误分类按通用模式建（超时/拒连/context overflow/空回复/JSON 崩），部署环境实测再调。本地 vllm（port 1028）本 session 未必起得来，重试/降级逻辑按分类设计，实测调参留部署环境

## 下游

- → 进 PLAN（同 slug `session-continuity-local-resilience`）
