# 需求对齐卡：G25a 装配工艺卡片「相辅相成」生成模型 + LLM 空响应前置修复

## 背景（问题怎么来的）

2026-07-23 内网部署（F 盘 gywj-0.3，qwen3-30b-a3b）补齐工作流出三个问题：
1. 第一次任务卡很久 → 返回 "llm 读取失败"；
2. 普通问答（qa）模式没接检索；
3. 装配工艺卡片（G25a）生成出来，**「工序内容/工艺方法」列 10 行全空**，但辅料列有内容。

用户由此提出根本性质疑：**工艺方法和辅料/用料/设备应当「相辅相成」（强关联、必然一致），不该割裂**。核查代码后确认现状是割裂的——辅料独立 extract 直填，工艺方法 LLM 独立生成，两路无绑定。

## 目标

把 G25a 装配工艺卡片从「两路割裂」改成「相辅相成」生成模型，并先修复阻断地基的 LLM 空响应。

### 新生成模型（用户设计）

1. **拆工序**：按用户上传的不完整工艺文件拆工序骨架（工序数/名）—— 现状已符合（`extract_assembly_steps`）。
2. **工艺方法（工序内容）**：检索素材库（知识库已有工艺文件）里**同类工序的现成工艺方法**作为套用素材 → **针对当前工件针对性生成**（不直接抄）。展示为利落连贯文字。
3. **辅料/用料/设备**：**不建新库**，用现有 HierarchicalContext 索引**按「栏/列」检索**素材库工艺文件的辅料栏/设备栏 → 对照工艺方法，把检索到的固定词句填进表格列（列 = 工艺方法里的「空」）。
4. **绑定一致**：工艺方法文字里自然用到这些辅料/设备，系统排布时把列空预留对齐填上 → 工艺方法提到的辅料 = 列里填的辅料，必然一致。

### 成功标准（可观察）

- G25a 每道工序「工序内容（工艺方法）」**非空**（修掉 Gap C 后）。
- 工艺方法文字里出现的辅料/设备 = 辅料/设备列填的内容（**一致性可抽查**）。
- 工艺方法**可溯源**到素材库现成工艺方法（套用素材，非凭空编）。
- 辅料/设备列内容来自素材库按栏检索，**不是** `structured_extractor` 从本次源文档 pattern 搬运。
- 不新增独立辅料/设备库（复用现有索引，单一数据源）。

## 边界

### 做
- **Gap C（前置阻断，最先）**：`generate_with_messages` 非流式适配 qwen3 推理模型——content 为空时不静默 success（降级流式重试 / 合并 reasoning_content / 显式报错）。让工艺方法能真正生成出来。
- **Gap A + B（核心，新模型）**：G25a 生成重构——套用素材库现成工艺方法针对性生成 + 按栏检索辅料/设备填列 + 表格列空绑定一致性。

### 不做
- 不建单独的辅料/设备/用料词句库（复用 HierarchicalContext 索引，防多库不同步）。
- 本轮不动其他 source-driven 章节（G4a/G5a/G18a/G12a/G14a/G22a 的 extract 直填保持）。
- 问题 2（qa 模式检索体验）本轮**不做**，单列后续。
- 暂不在 F 盘部署版直接 hotfix；落地方式（主仓改+打包 / F盘应急）待范围定后另决。

## 模糊点（进 PlanMode 确认，不阻塞对齐）

- [ ] HierarchicalContext 现在能否按「辅料栏/设备栏」检索？需增强按列/按 table_type 检索吗？
- [ ] 套用素材库现成工艺方法的**匹配依据**：按工序类型？按工件？按工序名相似度？
- [ ] 辅料/设备检索的**粒度**：整栏全量 vs 按当前工序上下文筛选。
- [ ] Gap C 精确修法：reasoning_content 合并 / 空 content 降级流式 / 两者结合——读 `generate_with_messages` 全貌后定。
- [ ] 一致性如何保证/校验：生成时同次结构化产出辅料清单 vs 生成后从工艺方法回抽匹配。

## 下游
- → 进 PLAN（同 slug `g25a-method-aux-bind`）。
- PLAN 第一步应是 Gap C（地基），再排新模型（A+B）。
