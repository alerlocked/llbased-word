# Web框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
websockets==12.0

# 数据库
sqlalchemy==2.0.23
alembic==1.13.0

# 任务队列
celery==5.3.4
redis==5.0.1

# 音频处理
pydub==0.25.1

# HTTP客户端
httpx==0.25.2
requests==2.31.0
aiohttp==3.9.0

# OpenAI SDK（用于通义千问文本生成）
openai==1.12.0

# DashScope SDK（用于通义千问语音识别）
dashscope==1.14.1

# 数据验证
pydantic==2.5.2
pydantic-settings==2.1.0

# 工具库
python-dotenv==1.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# 文档解析
python-docx==1.1.0
PyPDF2==3.0.1
pymupdf==1.23.8  # PDF转图片和图片提取
pdfplumber==0.10.0  # 增强表格提取
Pillow==10.2.0  # 图像处理
docx2pdf==0.1.8  # Word转PDF

# MinerU高精度表格解析 - 版本锁定
# 安装选项:
#   基础安装: pip install magic-pdf==0.7.6
#   完整安装(含VLM): pip install magic-pdf[full]==0.7.6
# 注意: RTX 5080需要CUDA 12.8版本的PyTorch
magic-pdf[full]==0.7.6

# 数据处理
pandas==2.0.0  # DataFrame操作和CSV导出
openpyxl>=3.1.0  # Excel文件生成（用于多Sheet导出）
beautifulsoup4>=4.12.0  # HTML解析（用于MinerU表格转换）

# 日志
python-json-logger==2.0.7

# LangChain相关依赖
langchain==0.1.0
langchain-openai==0.0.5
langchain-community==0.0.20
langgraph>=0.0.10
chromadb==0.4.22
tiktoken==0.5.2
duckduckgo-search==4.1.1

# 多模态Embedding (BGE-VL)
FlagEmbedding>=1.2.0
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0

# 测试依赖
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
