# 需求对齐卡：G25a/G18a/G14a 生成质量 v2（回退 N2 + 新需求）

> ⭐ **南天门升级试点**：本次 ALIGN 用**截图标注**对齐（不只文字）——下午"工序名称列"文字歧义导致 N2 方向错，晚上用户截图标注才消歧。

## 截图标注对齐（用户 2026-07-26 晚 5 张截图）
- **截图 193158（G25a）**：① 工序名称列填**钳/机/检验**（工种，下午 N2 改真名错要回退）；② 工序**内容**列每工步开头写**工序名**（装前准备：1.1 ...）+ 工艺方法
- **截图 193801/194013（G18a）**：source 列（来自何处）= **物料用于哪个工序**（生成环节哪里取料）；备注 = 主要配件写"在哪个工序"
- **截图 195037/195052（G14a）**：前两列（产品工号/表名封面信息）+ 数据区写齐

## v1→v2 纠错（下午 N2+N3 方向反）
- **N2 回退**：工序名称列 skel[k-1] 真工序名 → **回退** asm.name 工种（钳/机/检验）。真工序名移到工序内容开头
- **N3 改向**：G18a source "待补" → **改成** craft_kg 物料→工序 graph 取真值

## 目标
4 改动落实 + playwright 验证（G25a 工种+工序名打头 / G18a source+备注 graph / G14a 写齐）。

## 主要配件判定（用户拍：主物料+关键配件写，标准件+耗材不写）
- 标准件（part_name 含 螺钉/螺栓/螺母/垫圈/销 或 GB-T/QJB/HB 标准号）→ 备注空
- 耗材（布/乙醇/胶/润滑脂/漆/油 等辅料词）→ 备注空
- 其他（主物料+关键配件）+ craft_kg 命中（物料→工序边）→ 备注写工序名
- craft_kg 没命中 → 备注空（不臆造）

## 边界
- 不臆造（备注 graph 没命中留空 / G14a provenance 不放宽靠上游注入）
- N1（extract 跨页修复）保留
- 不改 craft_kg build（物料 triple 走现成"使用"branch）

## 下游
- → PLAN（slug `g25a-g18a-g14a-v2`）
