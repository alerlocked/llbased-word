# VISION · 工艺文件辅助编辑系统(localknowledgebase-word)

> 项目北极星。**愿景/验收 = 用户主权**,本页只记用户已拍板的;未定标 TODO。
> lead 第 0 步强制校准;由 `/vision` 引导补充。进度读 DEV-LOG/git 自动填。

## 北极星

- 工艺意图 → 标准工艺术语 → 工艺文件生成(内生软件,需求明确,保交付)
- 终点【候选，待用户在 localkb session 拍】：工艺人员用它，从工艺素材/意图生成**可直接使用的标准工艺文件**，省掉手工编写的大部分活
- 阶段验收粗形态（VISION 层 · 方向级，不钻实现）：
  - **完整**：一份工艺文件该有的核心章节/表都生成（整份文件视角）
  - **可用**：生成内容与素材吻合，关键信息（材料/工序/参数）不缺失
  - **可靠**：失败显式报错，不静默假成功（已治，验不回归）

> ⚠ **项目层待办**（不在 VISION 定，由 localkb lead 的 ALIGN/PLAN 拆）：
> 覆盖哪几张表 / 判官打分阈值 / 非空率门槛 / 冒烟脚本怎么跑。
> lead 第 0 步读到本节，把上面三个粗形态拆成**可观察的命令·打分·率**，落进 ALIGN-<slug>.md → PLAN-<slug>.md。
> VISION 只锁方向（完整/可用/可靠），不锁实现细节——细节是项目层主权。

## 阶段路径

### 阶段 0(进行中):单机生成链路打通
- 工艺文件 source-driven 生成(G25a 装配卡 + G4a/G5a 目录 + G10a/G12a/G14a 明细等),保交付质量
- 进度:DEV-LOG(35 feature 83%;G4a source-extract 2026-07-21 闭环)

### 阶段 1(规划中):内网部署 + 并发多终端访问  ← 新增 2026-07-21(用户定)
- **目标**:部署到单位内网后,具备**并发多终端访问能力**(多人能同时用)
- **现状差距**(2026-07-21 查清):当前**匿名单用户**——无认证/无数据隔离(数据按 material_id 共享);SQLite 单 worker,并发读 OK、**并发写撞锁**(`database is locked`);PDF 解析全局单队列
- **配套功能**:**待讨论齐备**(用户定)——候选:认证登录 / 用户数据隔离 / 并发承载(uvicorn 多 worker 或换 PostgreSQL) / 会话管理 / nginx 并发调优
- **验收**:待细化——多终端并发访问的可观察标准(并发数?隔离边界?性能下限?)

## 禁区

- PMF 阶段不过度工程(冒烟测试,上线准备期再补覆盖率)
- 内生软件,需求明确,保交付别过度复杂

## 进度

- 见 [DEV-LOG.md](DEV-LOG.md) / git
