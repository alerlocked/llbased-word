# 需求对齐卡:G25a 前后端契约对齐 + docx2pdf 固定

## 目标
- 解决谁的什么问题:G25a 装配卡后端生成的「检验」内容前端显示不出(前端无 inspection 列 + 历史 key 错位),以及 docx2pdf 转换工具昨天 3 方法全挂不可用。
- 成功长什么样(可观察):
  - G25a 装配卡前端正常显示每道工序的操作内容 + 对应的检验行(检验单独成行,工序名=「检验」),贴合真实工艺文件截图格式
  - 前后端 column key 有自动校验机制:改一边忘改另一边时 guard hook 告警,不再出现"装配卡空白"
  - docx2pdf 能稳定把 docx 转 PDF,固定成南天门可复用工具(不再"没这工具")

## 边界

### 做
1. **后端 G25a 检验行生成**:去掉模板 `inspection` 列,改为每道操作工序后生成「检验」工序行(`step_name=「检验」`,`content`=检验项)。检验点数量按工序复杂度(AI 分辨)。
2. **前端 G25a.ts**:不加列(保持 21 列布局),落地昨天已改的 key 修复(`content`/`aux_materials`/`instruments`)并 commit。
3. **契约校验脚本 + guard hook**:对比后端模板 column key vs 前端 layout dataColumns key,Edit/Write 后自动告警不一致。
4. **docx2pdf**:先实测探底(`taskkill` 清 Word 僵尸 → 带 timeout 重试)定方案,修复后固定成 skill。

### 不做(挡 scope creep)
- 不重排 G25a 前端 21 列布局(检验不单独成列)
- 不做生成质量优化(用户钦定后续单独 lead 周期)
- 不动 G22a 过程卡(已验证完全对齐)
- 不碰 PDF 导出模板结构(前端不加列,导出不受影响)

## 模糊点(已清零)
- ✅ 检验怎么处理:前端不动布局,后端生成检验行(用户确认 + 真实工艺文件截图验证:检验单独成行,工序名=「检验」,内容以「检验」标注)
- ✅ 对齐机制:校验脚本 + guard hook 自动告警
- ✅ docx2pdf 方向:先实测探底再定最终方案
- ✅ 范围:契约对齐 + docx2pdf;生成质量优化后续单独

## 质量要求(用户钦定)
- 检验行内容按工序复杂度:简单工序(如安装密封圈)1 个检验点,复杂工序(如延迟开关组合安装)多个检验点,AI 自行分辨
- 检验行工序名称固定=「检验」,内容以「检验」标注,对应回所属操作工序

## 下游
- → 进 PLAN(slug=contract-align)
