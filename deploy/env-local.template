# ============ Local Deployment .env Template ============
# Copy this file to backend/.env on the target machine
# cp env-local.template ../backend/.env
#
# Kylin x86_64 deployment:
#   PDF parsing: MinerU on CPU (local) or Qwen-VL-Plus (cloud)
#   LLM serving: MindIE on 300I Duo NPU (remote server)

# ============ Tier-Specific LLM Endpoints (MindIE on 300I Duo) ============
DASHSCOPE_API_KEY=not-needed
DASHSCOPE_BASE_URL=http://SERVER_IP:1025/v1

# Complex tier: Qwen3-14B
DASHSCOPE_BASE_URL_COMPLEX=http://SERVER_IP:1025/v1
MODEL_TIER_COMPLEX=qwen3-14b

# Simple tier: Qwen3-30B-A3B
DASHSCOPE_BASE_URL_SIMPLE=http://SERVER_IP:1028/v1
MODEL_TIER_SIMPLE=qwen3-30b-a3b

QWEN_TEXT_MODEL=qwen3-14b
DASHSCOPE_HTTP_API_URL=http://SERVER_IP:1025/v1

# ============ PDF Parsing Backend ============
# Options:
#   mineru     - MinerU pipeline on local CPU (high accuracy, ~1.3 pages/min)
#   qwen       - Qwen-VL-Plus DashScope cloud API (fast, requires internet)
#   qwen_local - Qwen2.5-VL on MindIE NPU (requires 300I Duo server)
VL_SERVICE_BACKEND=mineru

# MinerU config: HF mirror for China (required for first-run model download)
MINERU_HF_ENDPOINT=https://hf-mirror.com

# Qwen-VL cloud config (used when VL_SERVICE_BACKEND=qwen)
QWEN_VL_MODEL=qwen-vl-plus

# Local VLM config (used when VL_SERVICE_BACKEND=qwen_local)
VL_LOCAL_BASE_URL=http://SERVER_IP:1040/v1
VL_LOCAL_MODEL=qwen2.5-vl-7b

# ============ OpenAI Config ============
OPENAI_API_KEY=not-needed
OPENAI_API_BASE=http://SERVER_IP:1025/v1

# ============ Embedding ============
EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5
LOCAL_EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5

# ============ DeepSeek (disabled for local) ============
DEEPSEEK_API_KEY=not-needed
DEEPSEEK_BASE_URL=http://SERVER_IP:1028/v1
DEEPSEEK_MODEL=qwen3-30b-a3b

# ============ App Config ============
HOST=0.0.0.0
PORT=8000
DEBUG=true
