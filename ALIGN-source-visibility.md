# 需求对齐卡：画像来源标注 + 生成工艺文件附参考资料（来源可见性）

> slug: source-visibility · 2026-08-22 建 · 状态：对齐中

## 需求 1：画像来源标注（用户已拍板语义，2026-08-22）

### 目标
- 画像内容（triples / knowledge 条目）带**来源维度**（来自哪份素材），与专业分库、工作区来源隔离体系打通。
- 画像定位 = **跨素材通用经验**，每个项目可选取使用，非项目专属。
- 学习入口由系统预设控制（暂不给用户自选功能）；**当前冻结不学新内容**。

### 已拍板（不再讨论）
- 画像按专业分库（已有）；生成用哪个专业注哪个专业画像（已有 orchestrator:353）。
- 通用工艺原则（principles 细则）内容不变，跟画像体系走，不单独一套。
- 学习冻结：学习是手动入口，不点不学，现状即满足，无需改代码。

### 现状（2026-08-22 实查）
- `data/profiles/assembly.json` triples 条目结构 `{s, r, o, process}`——**无来源字段**；库级 `source_document_ids: ['1','2']` 只是清单不清准。
- 注入点 `writing_agent.py:1045` 全量注入 `self._profile.triples`，不看来源。
- KG 已按 `{doc_id}::` 前缀分源（PR #65），与本需求互补。

## 需求 2：生成的工艺文件附参考资料（用户 2026-08-22 提出）

### 目标
- 生成的工艺文件**末尾附"参考资料"**，明示本文件生成取用了哪些素材/标准——来源可见，可溯源。

## 边界

### 做
- triples/knowledge 条目加来源标注（数据模型 + 学习写入 + 存量回填）
- 生成注入按（待对齐的）取用语义执行
- 生成产物追加"参考资料"章节（素材清单 [+ 引用标准，待对齐]）

### 不做
- 用户自选画像经验的 UI（明确暂不做）
- principles 改造（通用细则不动）
- 解冻学习入口 / 自动学习
- KG 层改造（已完成，不动）

## 模糊点（已全部清零，2026-08-22 用户拍板）

| # | 模糊点 | 拍板结果 |
|---|--------|---------|
| 1 | 画像取用语义 | **B：不过滤全量注入**。画像=跨素材通用经验，所有项目按专业共享；来源标注仅溯源。学习端受控、取用端通用 |
| 2 | 参考资料内容 | **素材清单 + 数据出处**。用途=工艺员溯源"生成内容从哪来、有没有错"。不放引用标准（标准审查是下游人工审查的事，不归本软件）；但生成内容里的引用从哪份素材来的要可溯源 |
| 3 | 格式与挂点 | **纯文字列表，不走模板章节**（导 Word 后用户会删，别污染模板体系） |
| 4 | 存量回填 | **清空重学**（学习写入时带来源，弹体/摩托车各归位） |

## 下游
- → 进 PLAN（同 slug `source-visibility`）
