# 需求对齐卡：知识按来源隔离——勾选升级为项目工作区域 + 图谱来源分开录

> slug: `source-scoped-knowledge`
> 背景：2026-08-20 数据库治理评估发现三处多型号扩展隐患（物料表无型号维度 / KG 参数跨来源覆盖 / specialty NULL），用户拍板：不建"型号"维度（素材里没明确型号，靠猜不可靠），改用**来源（素材文件）作一等维度**。
> 现状（代码核实）：素材勾选功能**已存在**（MaterialsPanel checkbox → `reference_materials` 传内容文本 → 后端只拼 prompt），但知识查询层（检索/物料/图谱）不理会勾选，全库查——勾选是摆设。

## 目标

- **解决谁的什么问题**：将来大量型号、多专业工艺文件进知识库后，生成工艺文件时知识不跨来源串（物料名称错填、工艺参数张冠李戴）。
- **成功长什么样**（可观察）：
  1. 两份文件学同一规格不同参数值（如 M5 螺柱力矩 3.6 vs 4.2）→ 图谱里两个值都活着，互不覆盖；单测可验
  2. 项目勾选来源 A 时：B 来源的物料名称查不到（G18a 显示待补——用户拍板：漏勾=操作失误非 bug，宁缺毋滥）、图谱检索只返回 A 的节点
  3. 勾选持久化：选一次跟项目绑定，刷新/换会话不丢，之后生成检索自动按此范围；可改，改完按新范围往后做
  4. 未勾选任何文件（存量项目）→ 行为与现状一致（全量），全量 pytest 不回归

## 边界

### 做
- **勾选升级为工作区域**（前端小改）：MaterialsPanel 现有 checkbox 保留 + 加**文件夹级整组勾**（FolderTree 已有）；勾选结果（素材 id 列表）**持久化到当前项目**；生成请求传 **id 列表**（现在只传内容文本）
- **知识层按来源过滤**（后端核心）：检索（HierarchicalContext filters 加 sources）/ 物料查询（KnowledgeSearchService 按 source_doc 过滤）/ 图谱查询（`_search_knowledge_graph` 等）全部只认勾选来源；`extract_and_save` 去重键改为**同来源内**去重
- **craft KG 来源分开录**：节点/边带来源标记，跨来源不合并不覆盖（修"后学顶掉先学"）；同来源重学幂等更新
- **存量治理**：craft_kg **清空重学**（用户已知情同意：现有数据全来自 documents/1，重学即恢复，learn 链路现成）；删两个 0 字节空壳 DB（`data/craftdoc.db`、`data/database.db`，真库在 `data/database/craftdoc.db`，删前 grep 引用）

### 不做（显式挡 scope creep）
- 型号维度列（用户拍板：来源即型号代理，不单独建）
- 50 条 specialty NULL 存量回填（被来源过滤自然吸收，查询不再按专业硬筛，低价值不做）
- KG 可视化亮暗渲染（查询过滤即全部效果）
- 阶段 1 的认证 / 多用户数据隔离（另一个话题，不混）

## 模糊点

| # | 点 | 状态 |
|---|----|------|
| 1 | ~~勾选来源的 API 入口形态~~ | ✅ 已拍板（2026-08-20）：升级现有素材勾选（非新 API）——按文件夹+按文件勾、持久化到项目、请求传 id 列表；开工选好即工作区域，可改，改完按新范围往后做 |
| 2 | ~~存量 craft_kg 清空重学~~ | ✅ 已拍板（2026-08-20）：清空重学，本次重点把链路做好 |
| 3 | G18a enrich / 各检索调用点的来源参数具体穿法（selected ids 从 API 到各查询的路径） | → PlanMode 探索确认 |
| 4 | 数据层三问（exp-craft-kg-feed-from-learn）：数据从哪来=learn 链路现成；何时灌=learn 时按来源录；持久化=per-file 增量 save（现有机制不动） | ✅ 已明确，无空白 |
| 5 | 空 DB 壳删除前引用检查；勾选持久化存储位置（项目表字段 vs project_state） | → PlanMode grep/探索定 |

## 下游

- → 进 PLAN（同 slug `source-scoped-knowledge`）
