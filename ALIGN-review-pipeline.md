# 需求对齐卡：审查流 + 意图准入规则 + 最近产出快照

> 2026-08-16 开（接 session-continuity 后续 + 23:18 实测暴露三病灶）。背景：对话问"还需要补充吗"→ intent=unknown → 乱走生成分支重复生成 11 章 + "未找到初稿"报错 + review 无真实执行器（LLM 通识瞎评）。

## 目标

- 解决谁的什么问题：工艺人员**在对话里问审查类问题**（"有什么问题/还需要补充吗"），系统基于**事实**回答（模板对照 + 最近产出），而不是 LLM 通识猜测；对话中的修改需求能正确路由到修改流（同事在做的执行单元）；问答正常走 QA。**对话永不触发生成/补齐**。
- 成功长什么样（可观察）：
  - S1 问"生成的工艺文件，配套明细表还需要补充吗"→ 回答对照事实（如"G18a/b 已生成，source 列 12 行有值；对照模板无缺章"或指出具体缺口），**不触发任何生成**（日志无 executing_chapters_parallel）
  - S2 问"有什么问题吗"→ 分级问题清单（严重/警告/提示），每条有出处（哪个对照发现），零"基于通识"
  - S3 "修改 G25a 工序五"→ 路由到修改意图（同事执行单元未合入前：明确回复"修改功能建设中"或走现有 edit 链路，不误触生成）
  - S4 同项目新会话指代"刚才生成的那篇"→ 能取到最近产出快照（不再"未找到初稿"）
  - S5 生成/补齐只在按钮（generation_mode 入口）触发——对话入口有守卫断言，绕不过
  - S6 pytest 全量不回归

## 边界

- 做：
  - A. 意图分类强化：review/edit/qa 三类对话意图的识别与路由（unknown 率显著降）
  - B. **准入守卫**：对话入口禁止触发 generate/fill（合并为按钮专属完整工作流，本卡只守卫不合并——合并是另卡）
  - C. review-pipeline 执行器：四对照（模板章节清单/数据库/生成内容质量/用户需求覆盖），前三项机器算产事实清单，LLM 只做第四项且只能引用清单
  - D. 最近产出快照：`_persist_turn` 时把 structured_results 存项目 state（路径+摘要），QA/review 类问题路由到它
- 不做：
  - 生成/补齐合并成一个完整工作流（正向链路数据库直取那批，另卡）
  - "来自何处"列修正（用户已定：等正向链路）
  - edit 执行单元本体（同事在做，我们只留路由接口）
  - 前端大改

## 模糊点（2026-08-16 全部清零，用户拍板）

1. 【已拍①A】对话修改需求兜底 → **安全兜底**：识别出修改意图后回复"修改功能建设中，请用框选或按钮"，不碰文件；同事执行单元合入后接路由
2. 【已拍②B】review 对照范围 → **四对照一次做全**（模板章节清单 / 数据库材料·标准号有据 / 生成内容质量 / 用户需求覆盖）
3. 【接受的不确定性】意图识别准确率依赖本地小模型，unknown 可能仍有残余——守卫兜底（unknown → 追问澄清，绝不默认生成）
4. 【接受的不确定性】快照只存最近一次（不存历史版本），够用性留实测

## 下游

- → 进 PLAN（同 slug `review-pipeline`）
