# 需求对齐卡：补齐/生成前数据源门控（source-gate）

> slug: source-gate · 2026-08-22 建 · 状态：对齐完成（语义全部来自用户拍板原话）

## 背景考古（2026-08-22 查清）

- 状态机原有"暂停等用户确认"设计（draft_complete_now_paused）✅ 仍在
- **2026-05-23 commit `07b060e` 拆门**：理由="前端没有 requires_response 处理器，流程断的"，改成 auto-confirm（方案一出立即执行）——临时补丁从未装回
- 2026-08-17 review-pipeline 的 gate 挡的是"对话入口"，按钮路径 auto-confirm 保留
- 2026-08-22 事故 = auto-confirm（三个月的门洞）+ 无数据源章节静默走 LLM 编造（PR#68 半边工程）+ 分析报告虚假乐观文案，三者叠加

## 目标

- **素材不齐（模板章节在工作区素材中无数据源）→ 状态机必须停住，反馈到用户**，绝不自动生成（用户原话："门控不是开玩笑的，state 就得停，得反馈到用户"）
- 用户确认后才继续；确认后无数据源的章节**全部"待补"占位，一格不编**（用户原话："缺东西就要提，不能编"）
- 分析报告如实：有数据源章节 N 个 / 无数据源 M 个（列出），明示"继续则缺源章节为待补"

## 边界

### 做
- agent.py：拆掉 auto-confirm 短路——无源章节存在时停在 paused，发"数据源缺失报告 + 确认请求"给前端，等用户确认再执行
- 前端：确认交互（报告卡片 + 「继续生成（缺源章节待补）」/「取消」按钮），复用现有消息流卡片模式
- 无数据源章节的生成行为：待补占位，不调 LLM 编内容
- 分析报告文案改为如实分栏（有源/无源清单）

### 不做
- 有源齐全时不添堵（全章节有数据源 → 保持现行直通生成，不加确认步骤）
- 不改状态机本身结构（paused 态复用，恢复其本意）
- 不动检索/注入/骨架链路（PR #65/67/68 已闭环）

## 模糊点

| # | 点 | 状态 |
|---|----|------|
| 1 | 停的粒度：仅"存在无源章节"时停（齐=直通） | ✅ 按"素材不齐就停"原话推导，齐了不停 |
| 2 | 确认后无源章节=待补占位 | ✅ 用户两轮明确 |
| 3 | 前端确认形态：报告卡片+两按钮 | ✅ 复用现有卡片模式，实现细节 PLAN 定 |

## 下游
- → 进 PLAN（同 slug `source-gate`）
